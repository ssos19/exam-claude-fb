# 요구사항: 축구공 위치 기반 점유율 추적

## 환경
- Node.js, Next.js
- MySQL (mysql2 라이브러리 사용)

## 도메인 규칙
- 공 위치는 1차원(좌/우)으로만 표현한다.
- 팀은 두 개다: A팀(Anthro, 별칭 Ant), B팀(Bob, 별칭 Bike)
- 각 팀은 좌/우 중 한쪽 진영을 담당한다 (어느 팀이 어느 쪽인지는 추후 결정).

## 데이터 입력 방식
- 공 위치 값은 수동으로 하나씩 입력한다 (폼 또는 API를 통해).
- 이번 단계에서는 입력 방식의 상세 스펙(폼 UI, API 스펙)은 정하지 않는다. 추후 별도 논의.

## 공격 점유율 정의
- 공격 점유율은 "공이 상대 진영에 머문 시간의 비율"로 정의한다.
- 즉 A팀 공격 점유율 = 공이 B팀 진영(A팀 기준 상대 진영)에 있었던 시간 / 전체 추적 시간
- 점유 이벤트(누가 공을 소유했는지)는 이번 범위에 포함하지 않는다.

## 이번 단계 범위
- 이 문서(docs/requirements.md) 작성 및 커밋까지만 진행한다.
- DB 스키마, API, UI는 아직 구현하지 않는다.
